
const fs = require(fs);
let text = fs.readFileSync(types/index.ts, utf8);
text = text.replace(popupOfferText?: string;, popupOfferText?: string;
 popupOfferLinkText?: string;
 popupOfferLinkUrl?: string;);
fs.writeFileSync(types/index.ts, text);

